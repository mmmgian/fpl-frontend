{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "prebuild": "npx nuxi prepare",
    "build": "nuxt build",
    "start": "nuxt start",
    "preview": "nuxt preview",
    "vercel-build": "npx nuxi prepare && nuxt build"
  },
  "dependencies": {
    "nuxt": "^3.18.1",
    "@nuxtjs/tailwindcss": "^6.12.0"
  },
  "engines": { "node": ">=18.20 <23" }
}